// Phase 2: full data loading (accounts, snapshots, config, tags, groups)
// Stub for Phase 1 — returns immediately so the shell can render.
export async function loadAll(): Promise<void> {
  return;
}
